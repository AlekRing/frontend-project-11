import styles from './styles.scss';
import * as bootstrap from 'bootstrap';

const App = () => {
    const root = document.getElementById('root');
    const test = document.createElement('div');

    // test.classList.add('accordion');
    // test.classList.add('accordion-flush');
    // test.innerText = 'Accordion';

    // const dr = new bootstrap.Dropdown(root, test);

    // console.log(dr);

    // root.appendChild(test);
    return 'Hello, world!';
};

export default App;
